import React from "react";

const MCQAnswer = () => {
  const [questions, setQuestions] = React.useState([]);

  return <div>
    <button className="shadow-md rounded-xl px-8">
        Add Option
    </button>
  </div>;
};

export default MCQAnswer;
